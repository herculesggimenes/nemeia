import { readFileSync } from "node:fs";
const types = readFileSync(new URL("../contracts/spacetimedb/intelligence.ts", import.meta.url), "utf8");

const clients = [
  ["Subscription scope", "which world state and changes this client follows", "Each logical client selects relevant entities, components, relationships and executions from authorized world views. Include the dependencies needed for its task, not just a label such as backpack. Clients follow the same world with independent interests and progress; they do not compete for one shared queue. Filters narrow interest, not permissions. Changing scope requires a fresh matching view and review of active-step dependencies. Rows entering or leaving a query are not proof of a physical appearance or disappearance. Use the selected database's native subscriptions; no new transport or query language is needed.", "authorized world views + task interests → subscribed state and changes"],
  ["Per-client inbox", "latest state plus events that must be handled", "Reasoning clients buffer work behind their subscriptions. For replaceable state, keep changed entity keys and read the newest values at the next step. Preserve user messages, tool results and important transitions individually, including messages arriving outside world subscriptions. If an object appeared and disappeared between steps, its latest state is not a substitute for a required appearance event. A display-only client can render its subscribed cache directly; not every subscriber needs a durable inbox.", "coalesce state · retain events · never queue every camera update"],
  ["Context preparation", "compile just before inference begins", "Wait until the required subscriptions are ready. At a consistent committed-cache boundary, reserve an exact event batch and detach the relevant world projection. Add the authorized goal, constraints, available actions, current executions and selected conversation/tool history. Preserve missing values, evidence times and relevant versions. Bound tokens and retained bytes; defer excess work without silently dropping must-handle events. Freeze the input and record its context ID before the model starts. After a disconnect, resynchronize and review dependencies before proposing new physical work.", "subscribed state + goal + pending events + history → immutable step context"],
  ["Scheduling & interruption", "an update is not an instruction to infer", "Wake on user input, tool completion, meaningful changes or a deadline—not every sensor update. Start with one active reasoning step per logical agent. While inference runs, subscriptions keep the cache current and the inbox collects later work. Never edit an in-flight prompt. A cancelled task or invalidated target supersedes the step; reject late results even if cancellation fails. Physical stop bypasses this scheduler. UI clients render continuously, rules can react promptly, and Typesafe or LLM workers use task-specific cadences.", "90 pose updates during one step → one latest pose for the next"],
  ["Decisions & feedback", "subscribers propose; execution checks current authority", "A client may propose an action, ask for clarification or make no change. Subscription access never grants execution permission. Before acting, review the prepared context against current task dependencies and request admission using fresh evidence. Admission, controller claim and local limits still apply. Execution receipts and measured outcomes return through the same subscriptions and can wake the next step. Wake rules should distinguish relevant outcomes from the client's own bookkeeping writes to avoid a self-triggering loop.", "subscribe → prepare → decide → validate → execute → observe"],
  ["Durability & acknowledgement", "handled means a durable outcome, not read", "Persist must-handle events and client progress when restart recovery matters. Reuse retained domain events only when they contain the required occurrence; a subscription reconnect is not replay. Record the step outcome before acknowledging its exact event IDs, leaving newer arrivals pending. Failed or superseded steps retain unhandled events. Delivery retries reuse identities; re-evaluation after release gets a new step ID and must reconcile any prior physical attempt. Bound required-event queues with explicit admission/backpressure; dirty-key overflow can instead trigger a rescan. The worker-store schema and crash recovery are not implemented by the thirteen world tables.", "reserve batch → persist outcome → acknowledge batch"],
];

export function renderClients({section,codeRow,proseRow,region},number="06b") {
  return section("clients",number,"Client subscriptions","Subscriptions select what a client follows. Inboxes buffer what needs attention. Context is the frozen input prepared when that client is ready to decide—not a new copy of the world.",`<div class="abstraction-list">${clients.map((row,i)=>proseRow(i,...row)).join("\n")}</div><div class="object-list">${codeRow("object",0,"ClientSteps & PreparedStep","reasoning lifecycle behind a subscription","A worker-local design contract, not a subscription transport or required queue service. A prepare operation reserves events without acknowledging them. Recovery must reconcile the active step and durable outcome before retrying; a trace is not the progress ledger.",region(types,"clients"),"client-step-contract")}</div><p class="reference-note">Established patterns: <a href="https://doc.akka.io/libraries/akka-core/current/typed/mailboxes.html">actor mailboxes</a> and <a href="https://spacetimedb.com/docs/clients/subscriptions/semantics/">consistent subscribed state</a>. No actor framework or separate broker is required for the first implementation.</p>`);
}
