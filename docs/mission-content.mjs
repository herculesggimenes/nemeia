import { readFileSync } from "node:fs";
import { missionCode, missionLogCode } from "./mission-model-content.mjs";
const types = readFileSync(new URL("../contracts/spacetimedb/missions.ts", import.meta.url), "utf8");

const principles = [
  ["Description", "the outcome and context supplied by the World Master", "MissionSpec.description explains the work, like a quest briefing. It provides context, not a completion predicate. Objectives define what counts as success. Keep the accepted specification inline in the mission row, with an optional template pin for provenance. Validate known references. A search description need not identify an existing object: the agent uses the accumulated map and observations to investigate. A changed description or objective creates a new mission; changing strategy does not.", "description explains why · objectives define what must be demonstrated"],
  ["Objectives & dependencies", "outcome graph, not a fixed action script", "Each objective has a stable ID, a typed criterion and an all-of dependency list. Independent objectives can progress in parallel. Optional objectives do not block completion and cannot be prerequisites of required ones. Reject duplicate IDs, missing references, cycles, empty required sets and graphs above 32 nodes. These are milestones: acquiring geometry once does not mean it stays fresh forever. Continuous conditions remain action admission and local-control checks.", "geometry + semantics → approach → fresh geometry"],
  ["Objective progress", "validated evidence, not an agent's completion claim", "The world records one immutable completion proof per objective in mission_objective_progress. The view includes pending objectives too, derived from the specification. An observed criterion checks the target, requested facet, acquisition time and evidence access; it proves a measurement exists, not that a classification is certainly true. Motion criteria require a matching measured execution outcome. Located accepts an object finding under explicit World Master review of the match, position and supporting evidence. Repeated delivery cannot inflate progress. The same evidence can advance several objectives only after each validates independently.", "retained evidence + criterion-specific acceptance → objective progress"],
  ["Lifecycle & safe closure", "active → closing → terminal", "Creation activates an authorized specification. The requested object may remain unidentified while the agent investigates; movement still requires independent authority and qualified local evidence. All required credits request success; cancellation requests cancellation; a deadline requests failure. Closing blocks new admission and claim, requests cancellation of unfinished linked attempts and waits for confirmed safe closure. Terminal failure with an unknown physical outcome is not safe closure. Cancellation may replace pending success until terminal commit; once terminal, the mission is immutable. A deadline reached during closing also prevents success. A failed action does not automatically fail the mission: a fresh attempt is possible while active, but first reconcile the old attempt.", "goal achieved ≠ all physical work safely closed"],
  ["Mission log", "all assignments for one agent; the mission board", "A World Master creates missions, then assigns the agent responsible for them. One agent can hold multiple assignments. Its mission log joins those assignments with descriptions, objectives, progress and outcomes; it is not another writable mission store or an event history. The agent coordinates work across the log. Selecting a focus does not pause other missions, stop their deadlines or prevent independent evidence from advancing them. Assignment and Unit grants remain separate.", "World Master assigns → agent's mission log → selected work → shared progress"],
  ["Cross-mission coordination", "one agent loop, several active missions", "Nemeia prepares a compact summary of every assignment plus relevant changes, evidence, available Units and outstanding executions. The agent chooses its next work and reads details as needed. A blocked mission does not block the whole log. If no useful work is possible, wait for relevant changes or deadlines instead of repeatedly invoking the model. A Unit reservation applies across missions; switching attention is not physical cancellation. Eve owns the agent loop, not a separate loop per mission.", "all assignments visible · bounded detail · one coordinated decision loop"],
];

export function renderMissions({section,codeRow,proseRow,region}) {
  const references = `<div class="architecture-rows">
<div class="architecture-row"><strong>Definition vs progress</strong><div><a href="https://www.azerothcore.org/wiki/quest_template">AzerothCore quest definitions</a> and <a href="https://www.azerothcore.org/wiki/character_queststatus">character quest status</a> separate reusable content from a participant's progress. Nemeia adopts that distinction, with an inline immutable specification per mission.</div></div>
<div class="architecture-row"><strong>Objective composition</strong><div><a href="https://docs.eqemu.dev/server/task-system-guide/">EQEmu task activities</a> define typed goals, counts, optional activities and ordered steps. Nemeia uses typed milestones and explicit dependencies to describe required outcomes.</div></div>
<div class="architecture-row"><strong>Typed objective records</strong><div><a href="https://github.com/TrinityCore/TrinityCore/blob/master/src/server/game/Quests/QuestDef.h">TrinityCore's QuestObjective</a> separates objective identity, type, target and amount. Nemeia follows the typed-objective pattern but replaces gameplay credit with validated domain evidence.</div></div>
</div><p class="reference-note">These are primary references for open-source MMO server implementations, not claims about Blizzard's or Daybreak's internal databases. The Nemeia adaptation below is a design decision, not an MMO standard.</p>`;
  const definitions = [
    ["mission-criteria","ObjectiveSpec & criterion","explicit conditions with typed validators","Keep explanatory text separate from measurable conditions. Observed and approached use retained measurements and execution receipts. Located searches for an initially unknown object and requires World Master acceptance of a finding, not an LLM completion claim."],
    ["mission-spec","MissionSpec & Mission","description, objectives and durable lifecycle","The specification records description, objectives and constraints. Runtime validation enforces bounded graphs, known-reference integrity, nonempty descriptions and participant permissions. The requested object's identity and location may be unknown at assignment. Assignment is separate from creation."],
    ["mission-finding","MissionFinding","object match, location and supporting observations","Agents draft typed findings. This explicit review policy lets a World Master accept the object match and reported location while the world validates references, acquisition time and authority. The accepted finding lives in objective progress; drafts stay with Eve."],
    ["mission-evidence","ObjectiveProgress & MissionEvidence","pending or completed, with retained proof","Progress validation reads domain records inside the transaction. Unknown, pruned, out-of-scope or too-old evidence cannot complete an objective. After acceptance, the milestone remains completed; live action checks still use fresh evidence."],
    ["mission-graph","Example · approach and resample","parallel evidence and dependent outcomes","Resolve the entity before activation. Geometry and semantic evidence arrive independently; both unlock approach. A fresh observation after arrival satisfies the final objective. Navigation must meet the action's local spatial requirements."],
  ].map(([id,title,summary,description],i)=>codeRow("object",i,title,summary,description,missionCode[id] ?? region(types,id).replace('goal: "', 'description: "').replace('template: undefined, deadlineAt: undefined, ', ''),`object-${id}`)).join("\n");
  const log = codeRow("object",5,"MissionLog","one agent, multiple assigned missions","The mission board is a read projection, like a quest log. It does not duplicate mission state, assign Units or create an Eve session per mission.",missionLogCode,"object-mission-log");
  const research = `<details class="abstraction" id="mission-references"><summary><span class="abstraction-index">07</span><span><span class="abstraction-title">MMO references</span><span class="abstraction-summary">patterns borrowed and adapted</span></span><span class="toggle" aria-hidden="true"></span></summary><div class="schema-body">${references}</div></details>`;
  return section("missions","02b","Missions & mission log","Description explains the work. Objectives define success. The agent coordinates its assignments through a mission log. The world validates objective progress.",`<div class="abstraction-list">${principles.map((row,i)=>proseRow(i,...row)).join("\n")}${research}</div><div class="object-list">${definitions}${log}</div>`);
}
