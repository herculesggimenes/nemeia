import { readFileSync } from "node:fs";
const types = readFileSync(new URL("../contracts/spacetimedb/missions.ts", import.meta.url), "utf8");

const principles = [
  ["Definition & instance", "immutable intent; durable progress", "A Mission is an accepted objective graph, its World Master author, constraints and lifecycle. It survives client restarts and model changes. Keep the bound specification in the mission row; an optional template pin records authoring provenance. No template catalog is needed until reuse warrants one. Resolve entities and authorize the complete specification before activation. A changed goal or a repeat starts a new mission ID; a planner may change its strategy without changing the agreed outcome.", "author / resolve → authorize specification → active mission"],
  ["Objectives & dependencies", "outcome graph, not a fixed action script", "Each objective has a stable ID, a typed criterion and an all-of dependency list. Independent objectives can progress in parallel. Optional objectives do not block completion and cannot be prerequisites of required ones. Reject duplicate IDs, missing references, cycles, empty required sets and graphs above 32 nodes. These are milestones: acquiring geometry once does not mean it stays fresh forever. Continuous conditions remain action admission and local-control checks.", "geometry + semantics → approach → fresh geometry"],
  ["Evidence-backed credit", "one durable proof per objective", "The first slice supports acquiring a geometry/semantic facet for a bound entity and completing approach@1 for the required target and standoff, using any eligible assigned Unit. The reducer loads the retained observation or execution itself. It checks authorization, source association, objective readiness and evidence time; an LLM cannot submit a success boolean or increment a counter. One immutable credit per mission/objective makes redelivery harmless. Readiness begins at activation or the latest prerequisite credit. Observations must be acquired after readiness and still meet maxAgeMs; executions must be admitted after readiness and carry the matching mission link. Retain referenced evidence with the credit.", "domain proof → validated credit → derived ready/completed objectives"],
  ["Lifecycle & safe closure", "active → closing → terminal", "Creation activates an authorized, fully bound specification. All required credits request success; cancellation requests cancellation; a deadline requests failure. Closing blocks new admission and claim, requests cancellation of unfinished linked attempts and waits for confirmed safe closure. Terminal failure with an unknown physical outcome is not safe closure. Cancellation may replace pending success until terminal commit; once terminal, the mission is immutable. A deadline reached during closing also prevents success. A failed action does not automatically fail the mission: a fresh attempt is possible while active, but first reconcile the old attempt.", "goal achieved ≠ all physical work safely closed"],
  ["Mission subscriptions & authority", "one mission; many independent agents", "World Masters create missions and assign participants. Each agent follows shared objectives, evidence and outcomes through its own subscriptions and inbox. Agents can submit proof and propose actions, but cannot change the accepted outcome or grant themselves control. Admission and claim check participation, mission/objective pins, current Unit assignment, deadlines and installed policy. MissionSpec contains domain outcomes, not Unit IDs or motor limits. A mission-less request is reserved for audited World Master intervention, never an agent escape hatch.", "shared mission + independent agents + explicit Unit grants"],
  ["Small first slice", "borrow the pattern, not an MMO engine", "A mission row and immutable credit rows own goal progress. Mission participation, Unit assignments and messages add teamwork without a second mission copy per agent. There is no mandatory commander agent: add one only if the task needs coordination. Counted objectives still need distinct-item identities and deduplication. Dynamic branches, nested missions, mission pause/resume, rewards and reset calendars remain deferred; administrative agent pause is separate. Add typed criteria and validators for concrete outcomes, not arbitrary JSON success programs.", "shared credit ledger · independent inboxes · explicit authority"],
];

export function renderMissions({section,codeRow,proseRow,region}) {
  const references = `<div class="architecture-rows">
<div class="architecture-row"><strong>Definition vs progress</strong><div><a href="https://www.azerothcore.org/wiki/quest_template">AzerothCore quest definitions</a> and <a href="https://www.azerothcore.org/wiki/character_queststatus">character quest status</a> separate reusable content from a participant's progress. Nemeia adopts that distinction, with an inline immutable specification per mission.</div></div>
<div class="architecture-row"><strong>Objective composition</strong><div><a href="https://docs.eqemu.dev/server/task-system-guide/">EQEmu task activities</a> define typed goals, counts, optional activities and ordered steps. Nemeia uses typed milestones and explicit dependencies, keeping the first slice smaller than a general workflow engine.</div></div>
<div class="architecture-row"><strong>Typed objective records</strong><div><a href="https://github.com/TrinityCore/TrinityCore/blob/master/src/server/game/Quests/QuestDef.h">TrinityCore's QuestObjective</a> separates objective identity, type, target and amount. Nemeia follows the typed-objective pattern but replaces gameplay credit with validated domain evidence.</div></div>
</div><p class="reference-note">These are primary references for open-source MMO server implementations, not claims about Blizzard's or Daybreak's internal databases. The Nemeia adaptation below is a design decision, not an MMO standard.</p>`;
  const definitions = [
    ["mission-criteria","ObjectiveSpec & criterion","typed milestone and all-of dependencies","Only two validators are specified initially. An observed semantic facet proves that hypotheses were acquired, not that the hypothesis is true. An approached criterion requires a measured successful receipt, not merely a command."],
    ["mission-spec","MissionSpec & lifecycle","accepted intent and constraints","The SDK builders define the storage types; runtime validation still has to enforce bounded graphs, known targets, participant permissions and reference integrity. A mission is a first-class domain object built on the existing world foundations."],
    ["mission-evidence","MissionEvidence","proof references, not progress assertions","Credit validation reads domain records inside the transaction. Unknown, pruned, out-of-scope or too-old evidence cannot grant credit. After credit, the milestone remains achieved; live action checks still use fresh evidence."],
    ["mission-graph","Example · parallel checks, then approach and resample","four objectives, no separate task hierarchy","The entity is already resolved before activation. Geometry and semantic evidence can arrive independently; both unlock approach. A new geometry observation acquired after approach credit completes resample. Strategy and eligible Unit selection remain the agents' choice."],
  ].map(([id,title,summary,description],i)=>codeRow("object",i,title,summary,description,region(types,id),`object-${id}`)).join("\n");
  const research = `<details class="abstraction" id="mission-references"><summary><span class="abstraction-index">07</span><span><span class="abstraction-title">MMO references</span><span class="abstraction-summary">patterns borrowed and adapted</span></span><span class="toggle" aria-hidden="true"></span></summary><div class="schema-body">${references}</div></details>`;
  return section("missions","02b","Missions & objectives","A mission states what should be achieved and what proves it. Clients choose how to advance it; the world owns its specification, progress and lifecycle.",`<div class="abstraction-list">${principles.map((row,i)=>proseRow(i,...row)).join("\n")}${research}</div><div class="object-list">${definitions}</div>`);
}
