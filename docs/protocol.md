# Retired protocol draft

This earlier protocol is superseded by [the selected SpacetimeDB protocol](./protocol-spacetimedb.md).
The executable authority is the [module source](../contracts/spacetimedb/src/index.ts)
and its [generated client](../world-client/src/generated/index.ts).

The prototype WorldService/journal and custom transport are not a supported
runtime or compatibility path. Their historical tests and source remain on disk
pending separately reviewed package retirement; Git history is preserved.

Use the [runbook](./implementation-plan/RUNBOOK.md) for selected commands and
[execution status](./implementation-plan/EXECUTION.md) for qualification evidence.
