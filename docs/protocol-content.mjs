// Descriptions and storage design. Types/example code come from contracts/protocol.
export const foundations = [
  ["World", "one committed state", "Owns entities, relationships, installed actions and a journal cursor. A snapshot is detached and internally consistent.", "worldId + cursor + entities + relationships + actions"],
  ["Entity", "identity, not a class hierarchy", "An opaque identity, a display label, and typed components. Two backpacks are two entities even when their labels match.", "entity_backpack_01 ≠ entity_backpack_02"],
  ["Component", "a typed domain value", "A value plus revision and observation evidence where needed. UI strings and colors are derived separately.", "core.geometry → { version, value, evidence }"],
  ["Relationship", "a directed fact", "A structural subject–predicate–object tuple. Both endpoints must exist. Removing an entity also removes its incident relationships.", "robot_go2_01 — observes → entity_backpack_01"],
  ["Affordance", "an evaluated possibility", "Derived from components, freshness and action requirements. It is a discovery hint, not authority; dispatch checks again with actual input.", "actor + target + action + evaluatedAt + availability"],
  ["Action", "meaning independent of implementation", "Declares requirements and input/output schemas. A binding chooses a simulator or physical executor without changing action meaning.", "robot.approach → measured standoff distance"],
  ["System", "a trusted producer or projector", "Ingests evidence or submits an atomic batch of changes. Association and fusion belong here, not in presentation code.", "ObservationSink → association → WorldWriter"],
  ["Event", "a committed fact", "A CloudEvents envelope with typed Nemeia data. Journal position is separate from stable identity. World changes retain complete values.", "CloudEvent + { cursor, recordedAt }"],
];

export const objects = [
  ["world", "WorldSnapshot, Entity & Change", "the complete world-view structure", "Scene and RobotWorldView are projections of this snapshot. Component keys determine value types; changes include explicit removals and commit atomically."],
  ["evidence", "Observation, FrameRef & Resource", "evidence without ambiguous artifacts", "A frame is a transient sensor sample. A resource is retained bytes with a known schema. Semantic and geometric facets keep independent acquisition times; association uses scoped tracks, never labels."],
  ["geometry", "Pose & Geometry", "ROS-derived meaning, explicit JSON mapping", "Units and axes follow REP-103/105. A box has a center pose and full extents. boundingBox3D is Nemeia’s union tag, not a ROS wire type. Incomplete or untransformable geometry stays unknown."],
  ["actions", "Action, Affordance & Execution", "one action lifecycle", "One request creates one execution. The host resolves the executor and pins component versions. Accepted is not completed; physical results require measured evidence."],
  ["events", "CloudEvents & journal pages", "standard envelope, local ordering", "CloudEvents defines the envelope. Nemeia defines data payloads and causal event IDs. Acquisition time is not receipt time; cursors resume within one world, not across arbitrary logs."],
  ["streams", "SensorFrame & RobotStatus", "live device evidence", "Sensor payloads use bounded streams, not high-rate journal writes. Device reception time is not the time status() was called. Sent, device acknowledgement and semantic completion are distinct."],
  ["control", "ControlAdmission & reports", "local physical authority", "The host supplies limits. The local gate owns exclusivity, monotonic watchdogs, cancellation and stop. A latch is reported separately from physical safe-state confirmation."],
  ["executor", "ExecutorContext & result", "a scoped execution attempt", "The executor receives a snapshot, cancellation signal, allowed streams and optional scoped control session. It returns a result; the host owns lifecycle and committed changes."],
  ["journal", "Commit", "one atomic decision", "Request identity, principal, operation, fingerprint, events and original response commit together. Same request returns its original receipt; a changed request conflicts."],
  ["governance", "Admission & remote grant", "optional deployment features", "Approval adds a state to the same execution. A remote controller can accept a compact JOSE JWT with execution, robot, limits and epoch claims. Local plugins do not need signed grants."],
  ["common", "Identity, schemas & failures", "shared primitives", "IDs are opaque. Package hashes pin implementation; schema references pin value meaning. HTTP errors use RFC 9457. RequestContext is injected by authentication, never supplied in request JSON."],
];

export const services = [
  ["WorldService", "public domain API", "Reads world state, derives affordances and owns the execution lifecycle. The service instance fixes world scope. Authentication supplies context; HTTP/CLI are adapters, not separate policy engines."],
  ["ObservationSink", "trusted perception ingress", "Validates producer evidence and deduplicates observation identity before journaling. Retained resources must exist before their references are acknowledged."],
  ["WorldWriter", "trusted atomic changes", "Checks schema, ownership, component versions and the prospective final state. A stale cursor conflicts. One invalid change rejects the whole batch."],
  ["ActionExecutor", "one pinned implementation", "Executes one action attempt with a Promise result, AbortSignal and progress callback. State mode proposes changes; physical mode reports measured output. Worker isolation is a host responsibility."],
  ["RobotDriver", "device transport adapter", "Owns vendor protocols and normalized sensor/status payloads. Only the gate invokes motion commands. Existing Go2 transport or a ROS action adapter can implement the boundary."],
  ["ControlGate", "required for physical motion", "Acquires one session per robot, enforces local caps and stops without depending on the world database or agent. This is a local authority boundary, not an optional governance feature."],
  ["ControlSession", "bounded planar motion", "Sequences commands, rejects old/changed duplicates and enforces a local watchdog. Closing enters safe state. Normal completion releases the session without setting the emergency latch."],
  ["Journal", "transactional durable profile", "Persists complete decisions and original command receipts. Reads ordered pages. A database adapter supplies transactions and unique constraints; no custom consensus protocol is needed."],
  ["ResourceStore", "only when bytes are retained", "Stores immutable content and encoding metadata. Durable events contain IDs, not filesystem paths or expiring download URLs. Missing bytes are an explicit error."],
  ["AdmissionPolicy", "optional policy hook", "Allows, denies or requests approval using the same request and snapshot. Recheck current evidence and policy before approving. Local physical limits remain mandatory regardless of this hook."],
  ["ApprovalService", "optional human decision", "Decides the existing execution; it does not introduce a second Run or Authorization lifecycle. Authenticate the approver and apply the same durable retry rule."],
];

export const tables = [
  {name:"events", summary:"core · committed CloudEvents", description:"Authoritative decision journal. Position identifies a commit; ordinal orders events within it. Allocate positions under a per-world transaction lock. Readers never split a commit across pages.", constraints:"PRIMARY KEY (world_id, position, ordinal); UNIQUE (event_id). All events and the command receipt commit in one transaction. Retain full validated envelopes; IDs match envelope.id.", columns:[
    ["world_id","text","World scope; opaque ID."], ["position","bigint","Positive per-world commit position; not a public numeric cursor."], ["ordinal","integer","Zero-based order within the committed decision."], ["event_id","text","Globally unique CloudEvent identity."], ["envelope","jsonb","Complete CloudEvents JSON, including typed data and original time."], ["recorded_at","timestamptz","Receiving journal time; does not overwrite acquisition or event time."],
  ]},
  {name:"command_receipts", summary:"core · retry identity and original response", description:"One request commits once. Authenticate before serving a receipt. Same identity and body returns the original response, not a new execution.", constraints:"PRIMARY KEY (world_id, request_id). Compare principal, operation and fingerprint on conflict. Keep for world lifetime; commit atomically with events.", columns:[
    ["world_id","text","World scope."], ["request_id","text","Caller retry identity."], ["principal_id","text","Authenticated caller; cannot be replaced by a body field."], ["operation","text","Domain operation, such as execution.dispatch."], ["fingerprint","text","SHA-256 of canonical operation, body, principal and world."], ["response","jsonb","Original validated response, including assigned execution identity."], ["position","bigint","Last committed position in the same transaction."], ["created_at","timestamptz","Receipt creation time."],
  ]},
  {name:"world_checkpoints", summary:"optional · startup acceleration", description:"Disposable snapshot of a committed world. Not a second authority or mutable scene store.", constraints:"PRIMARY KEY (world_id). Snapshot cursor and position must agree. Rebuild from events when reducer/schema pin changes.", columns:[
    ["world_id","text","World identity."], ["position","bigint","Last completely reduced commit."], ["reducer","jsonb","Pinned reducer package and state schema."], ["snapshot","jsonb","WorldSnapshot plus internal projection state needed to resume association."], ["created_at","timestamptz","Checkpoint time, not sensor time."],
  ]},
  {name:"execution_index", summary:"optional · query acceleration", description:"Current executions reduced from execution.changed events. Never write business decisions directly to this cache.", constraints:"PRIMARY KEY (world_id, execution_id). Apply in commit order; rebuild from journal. Index actor/state only if queries need it.", columns:[
    ["world_id","text","World scope."], ["execution_id","text","Stable attempt identity."], ["position","bigint","Commit position last applied to this execution."], ["execution","jsonb","Full discriminated Execution value."],
  ]},
  {name:"resources", summary:"optional · retained media metadata", description:"References immutable retained bytes. Upload and verify bytes before acknowledging a ResourceRef. Never store live frame handles as durable resources.", constraints:"PRIMARY KEY (id). Bytes and metadata immutable. Verify hash and size on ingestion; missing/expired bytes produce explicit errors. Storage keys are private to the store.", columns:[
    ["id","text","Stable resource identity."], ["schema","jsonb","Exact payload layout/encoding schema reference."], ["media_type","text","MIME type."], ["byte_length","bigint","Non-negative content length."], ["sha256","text","Verified content digest."], ["storage_key","text","Private object-storage key, never a durable signed URL."], ["created_at","timestamptz","Storage completion time."], ["expires_at","timestamptz, nullable","Retention deadline; null if retained indefinitely."],
  ]},
  {name:"deliveries", summary:"optional · durable attention output", description:"Use only when exact delivered bytes must survive restart. Ordinary clients can read events and manage their own cursor.", constraints:"PRIMARY KEY (world_id, consumer_id, delivery_id); UNIQUE (world_id, consumer_id, from_position). Lock consumer, write bytes and advance to_position atomically. Retry returns the same bytes.", columns:[
    ["world_id","text","World scope."], ["consumer_id","text","Authenticated delivery scope and filter identity."], ["delivery_id","text","Stable retry identity."], ["from_position","bigint","Exclusive previous delivery boundary."], ["to_position","bigint","Last scanned commit, including filtered-out events."], ["formatter","jsonb","Pinned deterministic formatter or model configuration."], ["body","bytea","Exact delivered UTF-8 bytes; not an ephemeral Map reference."], ["created_at","timestamptz","Delivery creation time."],
  ]},
  {name:"control_state", summary:"physical host · durable local latch", description:"Local controller state, independent of the world database. Startup defaults to latched/unknown; stop must still attempt device safe state if storage fails.", constraints:"PRIMARY KEY (robot_id). Atomically acquire exclusive active execution. Clearing the latch requires an operator and fresh device status. Restart changes epoch and never restores motion.", columns:[
    ["robot_id","text","Controlled device."], ["epoch","text","Current controller incarnation; invalidates old remote grants."], ["stop_latched","boolean","Durable stop intent; startup failure defaults true."], ["active_execution_id","text, nullable","Current exclusive owner; null when idle."], ["safe_state","text","unknown, entering, confirmed or failed; needs device evidence."], ["updated_at","timestamptz","Record update time; not a monotonic watchdog clock."],
  ]},
  {name:"control_receipts", summary:"physical host · no accidental re-actuation", description:"Local admission and latest command receipt. An exact transport retry returns the receipt, not another movement. Uncertain restart outcomes fail safely.", constraints:"PRIMARY KEY (robot_id, execution_id). Persist admission before actuation. Never automatically retry an uncertain physical send. Duplicate latest sequence does not renew the watchdog; older sequences fail.", columns:[
    ["robot_id","text","Device scope."], ["execution_id","text","Admitted execution identity."], ["epoch","text","Controller incarnation at admission."], ["admission","jsonb","Pinned executor and effective limits."], ["grant_id","text, nullable","Consumed JWT jti, only for remote admission."], ["last_sequence","bigint","Latest accepted sequence; zero before the first command."], ["last_command","jsonb, nullable","Latest requested and sent command values."], ["last_ack","jsonb, nullable","Recorded dispatch confirmation; null when outcome is uncertain."], ["terminal_report","jsonb, nullable","Closed/failed/stop outcome; immutable once reconciled."],
  ]},
];

export const examples = [
  ["example-setup","Install meaning and read the world","WorldSnapshot + action catalog","The world already contains a robot and its measured pose. This is an illustrative, type-checked fixture, not a live robot trace. No mission or remote authorization service is required."],
  ["example-observation","Receive camera and LiDAR evidence","FrameRef → Observation","Perception reports a tracked object with full geometry and semantic evidence. Frames need not be retained. When an adapter transforms raw coordinates, it must include the exact transform samples; this fixture starts in map coordinates."],
  ["example-event","Record evidence","CloudEvent → journal receipt","The envelope identifies the producer and event. The journal adds its own position/time without replacing acquisition time. Ingestion deduplicates observation identity."],
  ["example-world","Associate and commit world state","Observation → Entity + Relationship","The trusted projector associates track_27 with one entity and commits both changes atomically. A second backpack receives a different identity. Scene and UI read this same snapshot."],
  ["example-dispatch","Discover and request an action","Affordance → ActionRequest → Execution","The host rechecks actual input and current evidence, chooses the executor, pins relevant versions, then persists accepted plus the retry receipt. Approval may pause this same execution, but is absent here."],
  ["example-control","Execute under local limits","Execution → ControlSession → RobotDriver","One local owner sends bounded velocity commands while independent timers watch liveness. The sample report says sent, not applied or completed. Further commands and fresh observations are required throughout motion."],
  ["example-completion","Confirm measured completion","New evidence + closed session → result","The fixture assumes fresh robot/target measurements and driver confirmation of safe state. The target did not move just because an action requested it. A timeout or unknown outcome would fail, not succeed."],
  ["example-resume","Consume and resume","Terminal event → UI / attention / replay","All readers share event IDs and resumable cursors. A retry returns the original dispatch receipt; query execution for current state. This compact fixture omits intermediate events, not the requirement to record them."],
];

export const decisions = [
  ["One state owner", "World replaces the separate Scene authority", "Entity/component state, scene queries and UI views derive from one WorldSnapshot. Display strings are not domain values. This removes competing object registries and projection-specific action catalogs."],
  ["One execution", "Action + executor, not Run + Authorization lifecycles", "Keep action meaning separate from its implementation. One execution moves through accepted, running and terminal states. Approval is optional pre-admission; a signed grant is only a remote transport credential."],
  ["Atomic decisions", "transaction + receipt", "Validate all changes before committing. Store original response and events together. Retry identity is durable and body-bound. Database transactions cannot roll back physical motion; uncertain actions are never blindly repeated."],
  ["Honest evidence", "identity, independent times, complete geometry", "Never associate objects by their label alone. Keep acquisition time separate from storage time and semantic freshness separate from geometry freshness. Missing dimensions stay unknown; no default obstacle boxes."],
  ["Local control", "mandatory only when hardware moves", "One gate owns limits, sequence, deadlines, cancellation and stop. Stop does not wait for the agent, worker cleanup or world journal. Command dispatch, device acknowledgement and physical success are different facts."],
  ["Replace, do not migrate", "greenfield implementation", "This is the canonical target, not an adapter over old APIs. Remove replaced prototype modules and databases as new slices land. There are no compatibility modes, dual writes or historical decoders to maintain."],
];
