export { v0ExampleRows as exampleRows } from "./world-view-content.mjs";

export const ownership = [
  ["World", "durable knowledge across local views", "The world owns committed entities, evidence, progressive local maps, missions and executions across worker and agent resets. One logical world does not imply one coordinate frame. Original local observations retain their provenance when Units reconcile spatial views. Missing alignment does not prevent useful local knowledge.", "one durable world · independent local frames · evidence-backed reconciliation"],
  ["Entity", "identity independent of labels and trackers", "An entity identifies a robot, object or other thing across observations. A new classification does not rename it. Association uses evidence to connect producer-scoped tracks to stable identities; matching labels alone are not enough.", "backpack-A remains the same entity as its measurements change"],
  ["Unit", "an entity with controllable capabilities", "A Go2, robot vacuum, simulated robot or motorized door can be a Unit. A chair is normally just an entity. Unit is a capability-based role, not a second identity or a competing entity hierarchy. Installed action bindings make control possible; an agent assignment makes requesting those actions permitted. An offline Unit remains a Unit, but unavailable. The concrete protocol below implements approach@1 as its first example, not every device capability.", "Unit ⊂ Entity · capability ≠ permission ≠ availability"],
  ["Agent", "a logical decision-maker, independent of hardware", "An agent coordinates assigned missions and controlled Units using automatically prepared world context. Agents can collaborate through shared mission facts and addressed messages. Eve owns reasoning history; Nemeia owns durable world state. Perception, mapping and control systems work independently at their own rates.", "mission log + Unit authority + automatic world context"],
  ["World Master", "the privileged client that governs missions and assignments", "A human or authorized automation sets missions, assigns participating agents, grants Unit control and read scope, and supervises outcomes. Its client can inspect the whole recorded world, pause agents, reassign resources and intervene through audited operations. This is a permissioned role, not a mandatory singleton service or a special kind of LLM. It sees known evidence, not physical omniscience; it cannot falsify observations or bypass local safety.", "World Masters assign missions · agents coordinate Units · systems execute"],
  ["Component", "typed state with evidence and time", "Components describe facets such as pose, geometry, semantics and robot control. Each observed facet retains its acquisition time, version and provenance. Missing values stay unknown; a label update cannot make stale geometry fresh.", "entity + independently timed facets"],
  ["Relationship", "a directed fact between entities", "Relationships connect existing entities using a defined predicate. Observed relationships retain evidence; configured facts remain distinguishable. Removal must preserve reference integrity, rather than leaving dangling endpoints.", "subject + predicate + object"],
  ["Affordance", "what an unit could do in the current situation", "An affordance evaluates an action's requirements against current evidence at a known time. It is a useful read-only hint, not permission to execute. The action boundary repeats freshness, identity and policy checks when accepting a request.", "unit + target + requirements → availability and reasons"],
  ["Action", "intent with bounded execution and a measured outcome", "An action names an operation with typed inputs, requirements and an installed implementation. Each request has one stable execution identity. Simulation and physical bindings share intent; hardware additionally enforces local limits, stop and durable receipts. A sent command is not a successful outcome.", "intent → admission → execution → evidence"],
  ["System", "software for a specific job, at its own cadence", "Perception, association, classification, pathfinding and control adapters can use specialized models or deterministic logic. They do not each need an agent, a mission or a reasoning loop. Agents choose intent at a higher level; systems turn accepted intent into plans, bounded execution and measured feedback. Inference and physical IO remain outside transactions. An interface does not require a separate microservice.", "observation clock ≠ decision clock ≠ physical control clock"],
  ["Event", "a recorded occurrence, not just the latest value", "Events preserve important transitions and outcomes that current state alone may hide. Clients retain must-handle events until acknowledged. World audit, client attention and diagnostic traces have different retention needs; an update callback is not durable history. Full scene replay needs a separately specified recorder.", "current state ≠ important transitions ≠ diagnostic traces"],
  ["Mission", "description, objectives and validated progress", "A World Master describes the work, defines objectives and assigns an agent. One agent coordinates multiple assignments through its mission log. Mission definitions and objective progress belong to the shared world, not a model conversation or a second board database. Validated evidence advances objectives; linked physical work must close safely before the mission ends.", "description + objectives → assignment → mission log → objective progress"],
];

export const objectRows = [
  ["values", "geometry", "Geometry & sensor references", "native SDK algebraic types", "The four geometry names are Nemeia tags, not an external standard. The SDK encodes union values as {tag,value}. Geometry follows ROS coordinate and unit conventions; reducers still validate finite values, positive sizes and quaternion norms."],
  ["values", "evidence", "Observations & provenance", "independently timed, partial facets", "Evidence retains acquisition times, source frames, exact transforms and fusion predecessors. Identity comes from authenticated association. Absent facets do not erase current components; delayed evidence must not replace newer samples."],
  ["values", "action", "Action request & result", "one attempt, one receipt", "The approach@1 contract illustrates a typed action with explicit inputs and admission checks. It may serve a direct approach objective or a scoped investigation, but arrival alone cannot complete a search or inspection. Each installed action follows the same intent, execution and receipt boundary. A running row means claimed; it does not prove physical motion."],
  ["contracts", "world-view", "World view & affordances", "durable local knowledge, read-only projection", "WorldView exposes accumulated entities, independent local views and mission progress. Persist underlying records and map resources, not a second WorldView snapshot. Adapters preserve timestamp, identity and revision semantics when serializing the projection."],
  ["planning", "local-map", "LocalMapView & checkpoint manifest", "progressive, retained and recoverable", "A local map grows from observations and optionally metric chunks. Current rows and immutable checkpoint manifests have separate roles. Restore both after restart; do not confuse retained map state with a current localized Unit. These interfaces specify the intended storage boundary."],
  ["contracts", "mission-view", "MissionView", "description, objectives and derived objective progress", "Join the authorized mission, assignment and mission_objective_progress records. Include pending objectives even when they have no progress row. Derive readiness using the same graph semantics as admission; these fields are hints, not permission."],
  ["contracts", "unit-agent-view", "UnitView & AgentView", "controllable entities and independent decision-makers", "Units reuse entity identity. Agents have separate durable identities, mission participation and resource assignments. One command-owning agent holds the Unit grant; observers and advisers do not need control ownership."],
];

export const contractRows = [
  ["world-memory", "WorldMemory", "durable local map and recovery", "The world stores frame-qualified knowledge and retained checkpoint heads. A trusted mapper/storage boundary verifies evidence and immutable bytes before a compare-and-set commit. SLAM and blob storage stay outside database transactions; local mapping does not require a global frame."],
  ["missions", "Missions", "accepted specification → verified progress → safe closure", "World Masters create and cancel missions. Assigned active agents can contribute validated proof to the shared ledger. Validate bounded graphs, resolved references/scopes and future deadlines. Located and inspected findings require the specified World Master review; hardware choice and motion limits stay outside MissionSpec. Reconciliation is idempotent and scheduled even without sensor writes."],
  ["world-masters", "WorldMasters", "mission teams, read scope and Unit assignments", "Only an enrolled World Master may administer agents and mission participation or grant Unit control. Bootstrap platform credentials remain the administrator's job. Revisions fence stale proposals. Revocation, pause and grant expiry block new admission/claim and request cancellation of affected attempts. A transfer retains the execution reservation until confirmed local closure; timers enforce expiry even without incoming observations. No new Unit owner can steal running hardware."],
  ["coordination", "AgentCoordination", "addressed team messages", "Messages carry durable bounded text between authorized participants, never evidence or control grants. Subscription interests are derived by Nemeia from Unit assignments; agents have no subscription-management operation."],
  ["ingest", "PerceptionIngress", "observations → association and current components", "Authorize producer identity, package/session and owned facets. Compare identical retry bodies before assigning identities. Validate clock error, transforms, endpoints, supersedes references and monotonic facet times. Commit observation, track binding, supplied component changes and audit together."],
  ["request", "ActionRequests", "a valid request becomes one accepted execution", "Check the existing execution ID first: same caller/body is a retry, even after expiry; a changed request conflicts. New agent requests require current unpaused agent identity, mission participation/revision, a ready objective and an unexpired Unit assignment for approach@1 at the pinned revision. For approached, match the specified target and standoff. For located or inspected, validate that the action target is in the investigation area or a verified adjacent viewpoint; arrival does not complete those objectives. Then check deadline, live Unit/target, installed policy, stop state, fresh evidence and geometry pin. Reject a second nonterminal attempt for the same mission/objective. World Master interventions use an explicit audited path with the same physical gates."],
  ["claim", "ExecutionClaims", "reserve one Unit before physical IO", "Only the bound controller can claim. In one transaction recheck mission readiness/revision, current requester role, participation, agent pause, Unit assignment/revision/expiry, deadline, evidence and target pin; reserve the Unit and set running. Assignment revision and local controller epoch are different fences. Use acceptBy as the claim deadline too. An existing claim remains reconcilable after expiry; that never permits restarting cancelled work."],
  ["cancel", "Cancellation", "request safe closure, not instant physical stop", "The requesting agent may cancel its own attempt, including after assignment revocation; a World Master may intervene. Set cancelling and audit atomically. The controller confirms safe closure before terminal cancellation. Even an unclaimed attempt needs confirmation it never started; expiry alone is not proof."],
  ["report", "ControlFeedback", "project measured controller state", "Only the enrolled controller and current epoch can report. Reject stale/future reports. A remote false stopLatched value never clears the actual local latch. Keep unit reservations if safe state is unknown."],
  ["finish", "ExecutionCompletion", "measured outcome → immutable terminal receipt", "Verify controller ownership, epoch, unit/target measurement identities and freshness, applicable target-motion policy, requested tolerance and the local safe-closure receipt. Cancellation wins once cancelling is committed. A failed outcome with unknown safe state retains the robot reservation until reconciliation."],
  ["configuration", "WorldAdministration", "explicit provisioning and lifecycle", "Restrict enrollment and binding installation to administrators. Reducers enforce relationship integrity; there is no assumed SQL foreign-key cascade. Refuse entity removal while active executions depend on it. New control generations require a verified local stop and persisted local fence, not just an incremented database counter."],
  ["controller", "LocalController", "robot-local durability, limits and stop", "This boundary stays outside SpacetimeDB. Use an existing navigation stack/driver, OS supervision, local persistent receipts and a monotonic watchdog. After a crash, reconcile receipts before delivery. Do not time out and reassign a physical attempt while an old worker may still be acting."],
  ["resources", "EvidenceStorage", "retained bytes stay off the state path", "Media continues over the appropriate streaming transport. Retained images, masks, clouds and meshes use existing blob storage. Only validated immutable references enter the world; credentials and expiring download URLs do not become component values."],
];

export const tableNotes = {
  world_config: ["world identity, clock and awareness policy", "One singleton row. No mandatory global frame. World-managed awareness includes direct Unit observations; an optional radius adds spatial interest when localization permits. Changing a radius neither grants access nor changes entity identity."],
  member: ["caller roles, scope and producer provenance", "All base tables are private. Membership gates public views and reducer permissions. Bootstrap administration through deployment, never an open signup reducer."],
  entity: ["stable identity and lifecycle", "All component/relation writes validate live entity references. Tombstone removal atomically deletes current components and incident relations; never recycle IDs."],
  pose: ["current frame-qualified position estimates", "Key by entity and registered frame, not one global pose per entity. One authorized projection owner per facet/frame. Compare times only within the correct stream/frame epoch; a delayed poll is not fresh evidence."],
  geometry: ["local spatial or image geometry", "Preserve estimates in their own registered frame. A 2D box remains image-space evidence. An approach@1 action requires a usable local 3D target and transform to the executor; it does not require a universally shared world frame."],
  semantic: ["classification hypotheses", "Independent update frequency and timestamps. Scores must be finite and in [0,1]. Do not interpret semantic confidence as geometry certainty."],
  relation: ["directed domain facts", "Reducers check both endpoints and installed predicate meanings. JSON tuple encoding avoids delimiter collisions. Evidence references are optional only for configured facts."],
  observation: ["bounded evidence and retry history", "Record association and provenance once. Identical producer/body retries are no-ops. Archive before pruning references needed by current components or decisions; reject replay outside the retained retry horizon."],
  track: ["producer-scoped association", "Tracking restarts use new sessions. No same-label merges. Prune dead sessions only when doing so cannot permit an old observation to create a duplicate entity."],
  action_binding: ["installed approach@1 policy per unit", "Each execution retains the accepted binding. Supporting multiple installed actions requires a composite Unit/action key and a typed contract for each action."],
  unit_control: ["one controller and active attempt per robot", "This is a database projection, not the emergency-stop authority. The local controller independently enforces the same fencing generation and can always stop without the database."],
  mission: ["accepted specification and durable lifecycle", "World Master authorship, shared team progress. Repeat/changed intent uses a new ID; strategy and assignment may change independently. Lifecycle or roster edits bump revision. Removal rejects targets referenced by active/closing missions. Unknown physical outcomes keep the mission closing."],
  mission_objective_progress: ["validated completion evidence for each objective", "One immutable row per completed mission/objective. Pending entries derive from MissionSpec; there is no writable status, percentage or duplicate counter. Validate retained proof and dependency readiness; preserve the first accepted proof on retries. The mission log joins this table with missions and assignments, without its own storage table."],
  agent: ["logical decision-maker and administrative configuration", "One agent can serve multiple missions and control multiple Units. Credentials belong to enrolled agent principals, not robots. Read scope, subscription interest and execution authority are separate. Scope/pause edits bump revision and invalidate affected contexts. No online flag is used as command authority."],
  mission_agent: ["many-to-many mission participation", "World Masters assign/revoke participation. Every edit compares and increments mission.revision. Revocation invalidates pending proposals and reconciles that agent's linked attempts; it does not erase shared credits or imply physical stop."],
  unit_assignment: ["one command-owning agent per controllable entity", "Grant allows named installed actions, not arbitrary telemetry writes. Revocation preserves a monotonically increasing revision. Reassignment/expiry cancels affected work but cannot clear unit_control.activeExecutionId. One command-owning agent holds the Unit grant."],
  spatial_frame: ["registered coordinate identities", "Local frames are first-class references, not a shared calibration assumption. A reset creates a new frame ID. Cross-Unit alignment requires qualified evidence; original observations retain exact local transforms and calibration provenance."],
  local_map: ["durable progressive local map", "Each local map records its originating Unit and root frame. Retain known and unlocated evidence even when it is no longer visible. Head metadata is small; large geometry stays in immutable storage. Current localization is a derived live status, not implied by this durable row."],
  map_revision: ["immutable retained checkpoint lineage", "Persist referenced bytes and evidence before atomic head advancement. Idempotency uses map ID and manifest digest; compare the expected revision for new commits. Restore a checkpoint plus later retained observations using recorded input coverage. A log of audit descriptions cannot restore a map."],
  agent_message: ["addressed coordination, separate from domain authority", "Validate current sender and recipient participation at submission; sender comes from identity. Read access is sender/recipient/World Master only and checked on delivery. Persist must-handle message meaning before acknowledging; bound bytes/rate and retention with explicit failure/backpressure. Chat is not a control grant, reservation or progress update."],
  execution: ["request, lifecycle and durable retry receipt", "The primary key deduplicates action delivery. Preserve terminal receipts for the supported retry lifetime; do not delete them and later reuse IDs. Physical outcome uncertainty blocks a new attempt."],
  world_event: ["persisted domain audit", "Ordinary table, not a transient event table or a bespoke replication stream. Retain a bounded history, archive when needed, and report unavailable history explicitly. This log alone cannot reconstruct every historical component value."],
};


export const failureRows = [
  ["Changed context or stale evidence", "review intent; recheck the action", "A cancelled goal, superseded step or lost target invalidates a proposal. A new pose sample does not automatically invalidate the whole reasoning step: re-evaluate relevant dependencies using explicit task rules, conservatively recomputing when equivalence is unknown. Read fresh evidence for the action request, pin its current geometry version and let admission/claim repeat strict checks. Inference deadlines are not physical evidence-age limits.", "reasoning validity ≠ permission to act"],
  ["Disconnect, retry or lost acknowledgement", "reconcile before acting", "Reconnect reconstructs current subscribed state, not every missed event. Use the execution row and robot-local receipt to determine what happened. Bound retries and disable offline queuing of new motion requests. Keep confirmed reads enabled for decision consumers.", "same execution ID · same immutable request"],
  ["Model uncertainty or API outage", "abstain; remain within local limits", "Use explicit none/unknown choices and qualified thresholds. Coalesce relevant changes, bound concurrency and prevent target oscillation. An LLM may propose a new plan, but receives no privileged execution path. Test selection in shadow mode before enabling autonomous requests.", "no new autonomous motion from a missing or expired answer"],
  ["Capacity and deployment", "one nearby world host first", "SpacetimeDB's documented tables are memory-resident with durable backing. Keep raw media elsewhere and bound history. Benchmark Nemeia's write fan-out, view cost, recovery, and p95/p99 latency; MMO use is evidence of a useful pattern, not a robotics real-time guarantee. The static Site is documentation, not the database host.", "measure the workload before expanding to a fleet"],
  ["License and multiple instances", "check deployment rights before fleet rollout", "The engine's current BSL additional grant restricts production use to one instance for an application/service, subject to its full terms. A database and an instance are different scopes. Review the selected release and commercial terms before independent robot/customer installations.", "source-available engine ≠ unrestricted multi-instance production grant"],
];
