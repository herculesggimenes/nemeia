const coordination = [
  ["Visibility, assignment & reservation", "three different permissions and state boundaries", "Visibility determines which recorded facts a client may read. Assignment determines which agent may request named actions on a Unit. Reservation records which accepted execution currently occupies that Unit. Seeing a Unit does not permit control; owning its assignment does not mean it is available. One agent owns command rights per Unit, with any number of authorized observers/advisers. Measured state is written by authorized systems, never by its assigned agent.", "read scope ≠ Unit control grant ≠ execution reservation"],
  ["Cooperation & messages", "shared facts first; chat for requests and explanations", "Agents coordinate through shared objectives, Unit assignments, active executions and measured outcomes. An addressed message can request help, propose a handoff or explain a decision. It cannot transfer control, reserve a Unit or declare an objective complete. Use the validated world operation for those effects. An optional coordinator agent is a strategy choice, not a required new role. The World Master remains responsible for changes to mission participation and authority.", "message proposes → authorized operation commits → subscriptions notify"],
  ["Reassignment, pause & expiry", "revoke authority without inventing physical closure", "A World Master can pause an agent or change its assignments. Bump the affected revision, invalidate pending proposals, block new admission/claim and request cancellation of affected attempts. Expiry follows the same rule, including when no new observations arrive. Retain the physical reservation until the local executor confirms safe closure; a new agent must wait. Assignment revision fences decision authority; controller epoch fences the physical executor. Neither alone can stop disconnected hardware, so local watchdogs remain required.", "revoke old rights → cancel/reconcile old work → safe release → new claim"],
  ["Three independent clocks", "RTS-level intent above continuous systems", "World Masters govern missions. Agents choose strategy and coordinate resources when useful. Perception systems continuously refresh evidence; pathfinding and control systems execute bounded actions at their own rates. A slow LLM does not slow localization or motor control. Fast classification need not create a new agent. Use task-specific measured cadences, meaningful-change triggers and timers rather than a single global tick or assumed fixed frequency.", "observation updates · deliberate decisions · robot-local control"],
];

export function renderAgents({section,proseRow}) {
  return section("teams","02c","Agents, Units & coordination","World Masters assign missions and Unit authority. Agents coordinate work through mission logs and shared facts; systems handle perception, mapping and execution at independent cadences.",`<div class="abstraction-list">${coordination.map((row,i)=>proseRow(i,...row)).join("\n")}</div>`);
}
