import { readFileSync } from "node:fs";
const types = readFileSync(new URL("../contracts/spacetimedb/intelligence.ts", import.meta.url), "utf8");
const agents = readFileSync(new URL("../contracts/spacetimedb/agents.ts", import.meta.url), "utf8");

const clients = [
  ["Subscription scope", "which mission and world changes this client follows", "Each logical client selects its mission, objective credits and relevant entities, components, relationships and executions from authorized views. Include dependencies needed for its ready objectives, not just a label such as backpack. Clients follow the same mission progress with independent inbox progress; they do not compete for one shared queue. Filters narrow interest, not permissions. Changing scope requires a fresh matching view and review of active-step dependencies. Rows entering or leaving a query are not proof of a physical appearance or disappearance. Use native database subscriptions; no new transport is needed.", "mission + authorized world views → subscribed state and changes"],
  ["Per-client inbox", "latest state plus events that must be handled", "Reasoning clients buffer work behind their subscriptions. For replaceable state, keep changed entity keys and read the newest values at the next step. Preserve user messages, tool results and important transitions individually, including messages arriving outside world subscriptions. If an object appeared and disappeared between steps, its latest state is not a substitute for a required appearance event. A display-only client can render its subscribed cache directly; not every subscriber needs a durable inbox.", "coalesce state · retain events · never queue every camera update"],
  ["Context preparation", "compile just before inference begins", "Wait until the required subscriptions are ready. At a consistent committed-cache boundary, reserve an exact event batch and detach the relevant world projection. Add the authorized goal, constraints, available actions, current executions and selected conversation/tool history. Preserve missing values, evidence times and relevant versions. Bound tokens and retained bytes; defer excess work without silently dropping must-handle events. Freeze the input and record its context ID before the model starts. After a disconnect, resynchronize and review dependencies before proposing new physical work.", "subscribed state + goal + pending events + history → immutable step context"],
  ["Scheduling & interruption", "many subscriptions; one reasoning step per agent", "Each subscription defines scoped interest, meaningful wake triggers, priority and batching/cadence. minIntervalMs is a rate cap, not a demand to poll; maxWaitMs is a desired batching bound after work arrives, subject to that cap and compute budget. All subscriptions feed one inbox and one durably leased step per agent, not competing inference loops. Freeze context just before inference. Reject late results after a cancelled mission, revoked assignment, pause or invalidated dependency. Local stop bypasses scheduling. Observation, decision and physical control clocks are independent.", "90 pose updates + 1 team message → latest state + retained message → one step"],
  ["Agent activity", "idle / ready / thinking / waiting", "Idle means no useful pending work. Ready means useful work awaits compute budget. Thinking means one reasoning step is active. Waiting means progress depends on an execution, resource, message or condition; new useful work can make it ready again. Administrative pause and transport connectivity are independent states. When all Units are unavailable, retain cheap availability/deadline subscriptions and stop repetitive inference. The agent may still coordinate, ask for help or analyse evidence without any Unit. Worker restart reconciles its lease and durable outcome before another step.", "idle → ready → thinking → waiting; meaningful changes can wake waiting agents"],
  ["Decisions & feedback", "subscribers propose; execution checks current authority", "A client may propose an action, ask for clarification or make no change. Subscription access never grants execution permission. Before acting, review the prepared context against current task dependencies and request admission using fresh evidence. Admission, controller claim and local limits still apply. Execution receipts and measured outcomes return through the same subscriptions and can wake the next step. Wake rules should distinguish relevant outcomes from the client's own bookkeeping writes to avoid a self-triggering loop.", "subscribe → prepare → decide → validate → execute → observe"],
  ["Durability & acknowledgement", "handled means a durable outcome, not read", "Persist must-handle events and client progress when restart recovery matters. Reuse retained domain events only when they contain the required occurrence; a subscription reconnect is not replay. Record the step outcome before acknowledging its exact event IDs, leaving newer arrivals pending. Failed or superseded steps retain unhandled events. Delivery retries reuse identities; re-evaluation after release gets a new step ID and must reconcile any prior physical attempt. Bound required-event queues with explicit admission/backpressure; dirty-key overflow can instead trigger a rescan. Worker inbox progress is not mission progress: its store and crash recovery remain separate from the world tables.", "reserve batch → persist outcome → acknowledge batch"],
];

export function renderClients({section,codeRow,proseRow,region},number="06b") {
  const definitions = [
    codeRow("object",0,"ReadScope & SubscriptionPolicy","permissions and interests are separate","World Masters grant visibility; policies select interests within it. Many policies with different cadences feed one agent inbox. Validate installed facet/trigger names and finite bounded settings; these are design types, not a new query transport.",region(agents,"agent-scope"),"object-agent-scope"),
    codeRow("object",1,"AgentRuntime","worker-local scheduling state","Persist the active-step lease and outcome with the inbox. The world stores administrative configuration, not a second scheduler. Optional UI runtime status may be stale and must never grant command authority.",region(agents,"agent-runtime"),"object-agent-runtime"),
    codeRow("object",2,"AgentSteps & PreparedStep","one coherent reasoning lifecycle per agent","A prepare operation reserves events without acknowledging them. Coordinate the lease across worker replicas. Recovery reconciles active step, durable outcome and execution receipts before retrying; a trace is not the progress ledger.",region(types,"clients"),"client-step-contract"),
  ].join("\n");
  return section("clients",number,"Client subscriptions","Every agent is a client with its own interests and cadence. Subscriptions select changes; an inbox retains useful work; context is frozen when that agent is ready. Display and system clients need not run a reasoning loop.",`<div class="abstraction-list">${clients.map((row,i)=>proseRow(i,...row)).join("\n")}</div><div class="object-list">${definitions}</div><p class="reference-note">Established patterns: <a href="https://doc.akka.io/libraries/akka-core/current/typed/mailboxes.html">message mailboxes</a> and <a href="https://spacetimedb.com/docs/clients/subscriptions/semantics/">consistent subscribed state</a>. No additional framework or message broker is required for the first implementation.</p>`);
}
