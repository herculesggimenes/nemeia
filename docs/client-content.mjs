import { readFileSync } from "node:fs";
const types = readFileSync(new URL("../contracts/spacetimedb/intelligence.ts", import.meta.url), "utf8");

const clients = [
  ["Per-client inbox", "latest state plus events that must be handled", "Each logical client has independent subscriptions and progress, not a competing consumer of one shared queue. For replaceable state, keep changed entity keys and read the newest values at the next step. Preserve user messages, tool results and important transitions individually. If an object appeared and disappeared between steps, its latest state is not a substitute for a required appearance event.", "coalesce state · retain events · never queue every camera update"],
  ["Context preparation", "compile just before inference begins", "At a consistent committed-cache boundary, reserve an exact event batch and detach the relevant world projection. Add the authorized goal, constraints, available actions, current executions and selected conversation/tool history. Preserve missing values, evidence times and relevant versions. Bound tokens and retained bytes; defer excess work without silently dropping must-handle events. Freeze the input and record its context ID before the model starts.", "goal + relevant world + pending events + history → immutable step context"],
  ["Scheduling & interruption", "one active reasoning step per agent", "Wake on user input, tool completion, meaningful changes or a deadline—not every sensor update. While inference runs, the inbox keeps collecting updates for later. Never edit an in-flight prompt. A cancelled task or invalidated target supersedes the step; best-effort cancellation is not enough, so reject late results as well. Physical stop bypasses this scheduler. UI clients can render continuously, while rules and model workers use their own cadence.", "90 pose updates during one step → one latest pose for the next"],
  ["Durability & acknowledgement", "handled means a durable outcome, not read", "Persist must-handle events and client progress when restart recovery matters. Reuse retained domain events only when they contain the required occurrence; a subscription reconnect is not replay. Record the step outcome before acknowledging its exact event IDs, leaving newer arrivals pending. Failed or superseded steps retain unhandled events. Delivery retries reuse identities; re-evaluation after release gets a new step ID and must reconcile any prior physical attempt. Bound required-event queues with explicit admission/backpressure; dirty-key overflow can instead trigger a rescan. The worker-store schema and crash recovery are not implemented by the thirteen world tables.", "reserve batch → persist outcome → acknowledge batch"],
];

export function renderClients({section,codeRow,proseRow,region},number="06b") {
  return section("clients",number,"Client inboxes & context","The world updates continuously. Each client prepares a deliberate view at its own pace; an LLM step does not need to consume every intermediate observation.",`<div class="abstraction-list">${clients.map((row,i)=>proseRow(i,...row)).join("\n")}</div><div class="object-list">${codeRow("object",0,"ClientSteps & PreparedStep","batch boundary, frozen context and acknowledgement","A worker-local design contract, not a required queue service. A prepare operation reserves events without acknowledging them. Recovery must reconcile the active step and durable outcome before retrying; a trace is not the progress ledger.",region(types,"clients"),"client-step-contract")}</div><p class="reference-note">Established patterns: <a href="https://doc.akka.io/libraries/akka-core/current/typed/mailboxes.html">actor mailboxes</a> and <a href="https://spacetimedb.com/docs/clients/subscriptions/semantics/">consistent subscribed state</a>. No actor framework or separate broker is required for the first implementation.</p>`);
}
