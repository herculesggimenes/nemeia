import { readFileSync } from "node:fs";
const types = readFileSync(new URL("../contracts/spacetimedb/intelligence.ts", import.meta.url), "utf8");

export const modelLayers = [
  ["YOLOE · frequent perception", "text / visual prompts → detections and optional masks", "Use YOLOE as the first candidate for the local, frequent perception path. Its open-vocabulary detection and segmentation accept text or visual prompts; prompt-free checkpoints are a separate mode. Start with a small checkpoint, a task-scoped class list and a bounded input resolution. Use a tracker between detections. This is a hardware-dependent choice to benchmark, not a promised frame rate.", "camera → YOLOE + tracker → PerceptionIngress"],
  ["SAM3 · selective refinement", "ambiguous or new targets → richer segmentation", "Keep SAM3 available for prompted segmentation and tracking when the fast path cannot resolve a target or needs a better mask. Schedule it on demand or at a lower rate, not as a prerequisite for every frame. Both paths produce the same observations. Neither a detector nor a mask alone supplies measured 3D geometry or stable world identity.", "selected frame / region → SAM3 → same observation boundary"],
  ["Perception scheduling & identity", "fresh evidence before throughput", "Keep one latest pending frame per stream and bound in-flight inference. Preserve acquisition timestamps and reject obsolete results; dropping queued frames must not make old evidence appear fresh. Tracking predictions are not new measurements. Share association across models; qualify cross-model matches instead of merging by label. Pin checkpoint, prompt profile and runtime in producer provenance. Benchmark end-to-end p95 latency, memory, dropped frames and task accuracy before setting rates. ONNX/TensorRT are optional optimizations; exported prompted classes may be fixed and require re-export when changed.", "YOLOE frequent · SAM3 selective · one association owner"],
  ["Spatial and audio processing", "depth, LiDAR, localization and speech → structured observations", "Calibrated depth/LiDAR and acquisition-time transforms supply measured geometry. Association maps tracks to entity identities. ASR supplies a transcript; the authenticated task boundary determines whether speech is an authorized request. Geometry calculations and known numeric checks stay in ordinary code.", "mask + calibrated depth + transform → boundingBox3D; audio → transcript"],
  ["LLMs and vision-language models", "interpret unfamiliar inputs; compose and revise plans", "A vision-language model may add semantic hypotheses with provenance. An LLM can interpret a goal, compose installed actions, draft focused decision questions and handle unfamiliar situations. Test and version these definitions before activation. Neither a generated description nor a plan changes permissions, invents an executor or overwrites measured state.", "goal + installed actions → proposed plan / versioned questions"],
  ["Shared world state", "one representation for all readers", "All producers submit evidence through validated ingestion and association. UI, rules, planners and Typesafe read the same entities, components and relationships. Build a small detached context for each decision; preserve timestamps, missing facets, competing hypotheses and evidence references. Do not maintain a separate Typesafe world.", "world → task-scoped DecisionContext"],
  ["Typesafe · bounded semantic decisions", "structured state → choice, score or yes/no probability", "Jev reads text/structured state produced by upstream tools. Use Choice for a known candidate set, Score for a defined rubric, and Noul for a yes/no probability. Independent questions can share one request; they do not see each other's answers. Apply the results in code, with abstention and task-tested thresholds. Noul has no separate confidence field.", "Which observed entity matches this goal? → candidateA | candidateB | none"],
  ["Rules, reducers and local control", "exact checks → admitted action → bounded execution", "Code checks identity, freshness, geometry, permissions and numeric limits. The world owner repeats those checks when admitting a request. Navigation and robot-local control execute the accepted action and enforce watchdogs and stop. Model outputs remain proposals. API outages or stale predictions cannot authorize motion.", "DecisionRecord → ordinary ActionRequest → executor → new evidence"],
];

export function renderIntelligence({ section, codeRow, proseRow, region }, number = "04") {
  const rows = modelLayers.map((row, i) => proseRow(i, ...row)).join("\n");
  const definitions = [
    ["context", "Decision context & record", "JSON projection of subscribed SpacetimeDB rows", "These are Nemeia design types, not Typesafe's internal model. Serialize bigint as decimal strings and Timestamp as UTC strings; never send SDK classes or Maps directly. A context is immutable for an evaluation. Revalidate goal, candidate membership and relevant row versions when a result arrives; unrelated world writes need not invalidate it."],
    ["evaluator", "TargetSelector", "one decision worker boundary; no actuator access", "A System subscribes to relevant state, coalesces meaningful changes and calls the selected provider outside database transactions. Bound concurrency and latency; discard superseded responses."],
    ["admission", "DecisionAdmission", "validate the result before proposing an action", "Map only options from the exact submitted context. Recheck the task, candidate set, evidence and deadline, then use the ordinary action boundary. A stable decision-to-request mapping prevents repeated delivery from creating new physical attempts. Reducers independently repeat current action requirements."],
    ["typesafe", "Typesafe HTTP contract", "typed request and response adapter", "The worker sends an authorized, minimal projection to the hosted API, subject to data-handling requirements. Pin the model and question version. Record probabilities without presenting confidence as correctness or a safety guarantee. This page contains no API key and makes no inference requests."],
    ["example", "Example · choose the requested backpack", "multimodal evidence becomes a focused question", "Illustrative context: segmentation/tracking supplies two backpack candidates; spatial processing locates them; a semantic worker supplies appearance hypotheses; speech processing supplies the request. The appearance and relationship summaries retain evidence in the world. Typesafe selects a candidate; code verifies that the evidence and task still hold."],
  ].map(([id,title,summary,description],i) => codeRow("object",i,title,summary,description,region(types,id),`intelligence-${id}`)).join("\n");
  const refs = `<p class="reference-note">Capability references: <a href="https://docs.ultralytics.com/models/yoloe/">YOLOE modes and export limits</a> · <a href="https://github.com/THU-MIG/yoloe">YOLOE research</a> · <a href="https://github.com/facebookresearch/sam3">Meta SAM3</a> · <a href="https://docs.typesafe.ai/primitives">Typesafe questions</a> · <a href="https://docs.typesafe.ai/confidence">Confidence semantics</a> · <a href="https://docs.typesafe.ai/models">Model versions and input limits</a>. These workers are implementation targets, not deployed capabilities. Check model and runtime licenses before distribution.</p>`;
  return section("intelligence",number,"Perception & decision systems","Use each tool for its task. Multimodal processing builds a shared world; Typesafe, LLMs and rules consume that world through the same decision and action boundaries.",`<div class="abstraction-list">${rows}</div><div class="object-list intelligence-definitions">${definitions}</div>${refs}`);
}
